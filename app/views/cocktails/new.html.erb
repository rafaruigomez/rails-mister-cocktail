<div class="container mt-5" style="width:450px">
  <%= simple_form_for(@cocktail) do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
      <%= f.submit %>
  <% end %>
  <%= link_to "Back to Cocktails", cocktails_path, class: "btn btn-primary mt-5" %>
</div>
